/**
 * constants/debugFixtures.stub.ts
 *
 * Stub de production — remplace debugFixtures.ts lors du build non-local.
 * Ce fichier ne contient aucune donnée de test.
 * Configuré dans metro.config.js via resolver.resolveRequest.
 */
export const DEBUG_ORDER = null;
